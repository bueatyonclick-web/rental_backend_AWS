[project]
name = "rental-backend"
version = "1.0.0"
requires-python = ">=3.12"

[tool.vercel]
entrypoint = "core.wsgi:application"

[tool.vercel.scripts]
build = "python manage.py collectstatic --noinput"
